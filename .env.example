# Database
DATABASE_URL=postgresql://whatsapp_bot:localdev@localhost:5433/whatsapp_bot
DB_USER=whatsapp_bot
DB_PASSWORD=localdev
DB_NAME=whatsapp_bot
DB_PORT=5433

# API
API_PORT=4000
NODE_ENV=development

# Auth
JWT_SECRET=change-me-in-production-use-a-long-random-string
JWT_REFRESH_SECRET=change-me-different-from-jwt-secret-min-32-chars

# Redis (required for webhook processing queue + cache)
REDIS_URL=redis://localhost:6379

# Encryption (32-byte hex key for AES-256-GCM)
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# WhatsApp (Phase 2)
WEBHOOK_VERIFY_TOKEN=my-webhook-verify-token
# META_APP_SECRET=your-meta-app-secret (used for global signature verification if needed)

# n8n Automation (Phase 4)
N8N_WEBHOOK_URL=http://localhost:5678/webhook/whatsapp-events
AUTOMATION_API_KEY=change-me-automation-secret-key

# AI Provider (anthropic | openai | gemini | none)
AI_PROVIDER=none
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=AIza...
# AI_MODEL=claude-3-5-haiku-20241022  # or gpt-4o-mini for OpenAI, gemini-2.0-flash for Gemini
# AI_TIMEOUT_MS=5000

# Email (Resend - https://resend.com)
# RESEND_API_KEY=re_...
# EMAIL_FROM=noreply@yourdomain.com

# Dashboard
NEXT_PUBLIC_API_URL=http://localhost:4000
